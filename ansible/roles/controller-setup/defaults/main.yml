---
helm_version: "v3.17.3"
kubeconfig_path: "/home/{{ ansible_user }}/.kube/config"

# Flux bootstrap — set these in inventory or pass via -e
flux_github_owner: "elizabetht"
flux_github_repo: "homelab"
flux_github_branch: "main"
flux_path: "clusters/homelab"
flux_github_token: ""   # pass via: -e flux_github_token=<PAT>
